apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: infra-hub
spec:
  accessModes: ["ReadWriteOnce"]     # one node mounts it read-write (fine for 1 DB pod)
  resources:
    requests:
      storage: 5Gi                   # small; plenty for audit logs

